server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Yönlendirme react-router'da; /hocalar gibi bir yolda sayfa yenilendiğinde nginx 404 vermesin.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
